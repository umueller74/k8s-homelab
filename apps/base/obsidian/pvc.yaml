apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: obsidian-config
  namespace: obsidian
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ""
  volumeName: obsidian-config
  resources:
    requests:
      storage: 10Gi
