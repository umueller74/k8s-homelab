apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization

metadata:
  name: esphome
  namespace: flux-system

spec:
  interval: 10m

  sourceRef:
    kind: GitRepository
    name: flux-system

  path: ./apps/production/esphome

  prune: true
  wait: true

  # Decrypt *.sops.yaml Secrets with the age key in flux-system/sops-age
  decryption:
    provider: sops
    secretRef:
      name: sops-age

  dependsOn:
    - name: infrastructure
    # Provides the NetworkAttachmentDefinition CRD used for the LAN address.
    - name: multus
