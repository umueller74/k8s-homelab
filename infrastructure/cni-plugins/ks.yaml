apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: cni-plugins
  namespace: flux-system

spec:
  interval: 10m
  path: ./infrastructure/cni-plugins
  prune: true
  wait: true

  sourceRef:
    kind: GitRepository
    name: flux-system

  timeout: 5m
